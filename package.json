{
  "name": "gridsome-shop",
  "private": true,
  "engines": {
    "node": ">=12 <13"
  },
  "scripts": {
    "build": "gridsome build",
    "develop": "concurrently \"gridsome develop\" \"yarn sanity\"",
    "explore": "gridsome explore",
    "sanity": "cd ./sanity && yarn start",
    "setup": "yarn env && yarn && cd ./sanity && yarn",
    "env": "cp .env.example .env"
  },
  "dependencies": {
    "gridsome": "^0.7.0",
    "gridsome-source-sanity": "^1.0.5"
  },
  "devDependencies": {
    "concurrently": "^5.2.0",
    "dotenv": "^8.2.0",
    "gridsome-plugin-tailwindcss": "^2.2.48"
  }
}
